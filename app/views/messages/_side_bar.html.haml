.side
  .side__header
    .side__header--content
      %h3.side__header--content-left
        = current_user.name
      .side__header--content-right
        = link_to new_group_path, class: "side__header--content-right-btn" do
          = icon('far fa-edit', class: "side_header--content-right-btn-icon")
        = link_to  edit_user_path(current_user), class: "side__header--content-right-btn" do
          = icon("fa fa-cog", class: "side_header--content-right-btn-icon-right")
  .side__contents
    .side__contents--main
      - current_user.groups.each do |group|
        .side__contents--main-top
          = link_to '#' do
            = group.name
        .side__contents--main-bottom 
          メッセージはまだありません。